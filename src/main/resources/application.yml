spring:
  data:
    redis:
      host: 10.100.104.24
      port: 6400
      password: DHD@ngh222
  session:
    data:
      redis:
        namespace: "session:gateway"
        save-mode: always
    timeout: 30m

  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: api-gateway
            client-secret: DQdBU5o7tk7EBbkncWcRD0ewjbpeDvKg
            authorization-grant-type: authorization_code
            scope:
              - openid
              - profile
              - email
        provider:
          keycloak:
            issuer-uri: http://10.100.104.24:8080/realms/donghee
            authorization-uri: http://10.100.104.24:8080/realms/donghee/protocol/openid-connect/auth
  cloud:
    gateway:
      server:
        webflux:
          default-filters:
            - TokenRelay
            - RemoveRequestHeader=Cookie
          routes:
            - id: test-service
              uri: http://10.117.9.40:8081
              predicates:
                - Path=/api/**
server:
  port: 8080
  forward-headers-strategy: native